# lava-azurerm

Autogenerated typed `(deflava-resource …)` shapes for every azurerm
terraform-provider resource. Mirrors pangea-azurerm.

## Regenerate

```bash
tofu providers schema -json > schema.json
lava-forge generate --schema schema.json --out resources
```
